#include <stdio.h>
int main()
{
    int a, i;
    while(i=1; i<=15; i++){
        printf("Enter age:");
        scanf("%d", &a);
        if(a<60 && a>40){
            printf("Age: %d\n", a);
            continue;
        }
        else(a>60 || a<40);
        {
                printf("Age is not in the range.\n");
        }
    }
    return 0;
}
